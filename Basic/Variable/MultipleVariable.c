//To Declare more than one variable of same type, use comma-sperated list.

#include<stdio.h>

int main(){

    int a=5, b=6, c=34;
    printf("Sum %d\n",a+b+c);

    //OR
    int x, y, z;
    x = y = z = 50;
    printf("Sum: %d\n", x + y + z);
}